# Install at atmoph-win-2-remote/.github/wiki-sync.yml
# Default branch: main. PUBLIC repo, so its wiki is public too: the
# network-identifier scan is enforced, and no private address, MAC, device
# UUID, or serial belongs in anything published from docs/.
auto:
  - glob: 'docs/*.md'
